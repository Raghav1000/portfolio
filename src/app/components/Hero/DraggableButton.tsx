import { motion } from "framer-motion";

const DraggableButton = () => {
  return (
    <div className="rotating-border flex py-1 px-5 text-sm gap-2 max-w-max rounded-full bg-opacity-20 border border-foreground/10 mx-auto">
      Welcome to my portfolio
    </div>
  );
};

export default DraggableButton;
